services:
  wiremock:
    build:
      context: ./wiremock
      dockerfile: Dockerfile
    image: clvn-wiremock:latest
    container_name: clvn-wiremock
    ports:
      - "8090:8080"
      - "8443:8443"
    restart: unless-stopped
